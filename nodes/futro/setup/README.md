Bring-up (2026-07-22) followed `../setup-plan.md` sections 3–4 directly — no separate
per-session setup docs were needed beyond that plan and `../status.md`. Add
`SETUP-NNN-*.md` files here if future futro-side setup work needs its own record,
mirroring the `nodes/workstation/setup/` convention.
