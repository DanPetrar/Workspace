No historical per-session setup docs exist for raspi in the old `Workstation` repo — the
Pi's own setup history wasn't tracked this way. Empty for now; add `SETUP-NNN-*.md` files
here as raspi-side setup sessions happen, mirroring the `nodes/workstation/setup/`
convention.
